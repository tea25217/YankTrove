#!/usr/bin/env bash
# Fail the push if the outgoing commits contain secrets or personal data.
# Git sends "<local ref> <local sha> <remote ref> <remote sha>" on stdin.
repo_root="$(git rev-parse --show-toplevel)"
exec "$repo_root/scripts/scan-secrets.sh" --push
