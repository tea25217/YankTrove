# TODO / 改善メモ

将来 GitHub リポジトリ作成後は、各項目を個別 Issue に切り出す想定。  
現時点ではリポジトリ未作成のため、ここにメモする。

---

## 既知の問題

### Chrome / Edge クッキー読み取り失敗（Windows）

- **現象**: 「使用するブラウザのクッキー」で Google Chrome（または Edge）を選択すると、`Could not copy Chrome cookie database` でリスト取得・ダウンロードに失敗する。
- **回避策（暫定）**: Firefox を使用する / Chrome を完全終了してから再試行 / Chrome 起動フラグに `--disable-features=LockProfileCookieDatabase` を追加。
- **背景**: Chromium が Cookie DB を排他ロックする既知問題（[yt-dlp#7271](https://github.com/yt-dlp/yt-dlp/issues/7271)）。
- **望ましい対応案（未実装）**:
  - UI 上で分かりやすい案内（起動中ブラウザの終了を促す）
  - cookies.txt 手動指定のサポート
  - ChromeCookieUnlock プラグイン等の調査・同梱可否の検討
  - Edge も同様に影響を受けることの明示

### バンドル ID の変更

- **現状**: `src-tauri/tauri.conf.json` の `identifier` が `com.yanktrove.app`
- **問題**: `.app` で終わる ID は macOS のアプリケーションバンドル拡張子と衝突しやすい、と Tauri が警告する
- **対応案**: `com.yanktrove.desktop` など、`.app` で終わらない ID に変更する
- **注意**: ID 変更後は OS 上で別アプリとして扱われる（既存インストールとは別物になる）

---

## 機能改善

### 動画キューへのフィルター機能

一覧（アーカイブキュー）にフィルターを追加する。

| 軸 | 内容 |
|----|------|
| 名前 | タイトルの部分一致検索 |
| 配信日時 | 期間指定（以降 / 以前 / 範囲） |
| 課金形態 | 公開 / メンバー限定、メン限はプラン別 |

実装メモ:
- リスト取得時にメタデータ（公開日時・availability / membership tier 等）を可能な範囲で取得・保持する必要がある
- yt-dlp の flat-playlist では情報が不足する場合があるため、詳細取得のタイミングを検討する

### 保存先のフォルダ分け（動画ごと）

現状: `YankTrove/{チャンネル名}/{タイトル} [{id}].*` を同一フォルダにフラット保存。

改善案: 動画ごとにサブフォルダを作成する。

```
YankTrove/{チャンネル名}/{タイトル} [{id}]/
  ├── *.info.json
  ├── *.ja-orig.vtt
  ├── *.mp4
  └── ...
```

### 取得データ選択に「整形データ（CSV）」を追加

- 「取得データ選択」にチェック項目を追加（形式は CSV を想定）
- 人間が開きやすい形に整形したメタデータ等を出力する
- 列案（仮）: 動画ID, タイトル, URL, 配信日時, 長さ, 公開状態/課金形態, 保存パス など
- 出力単位: 動画ごと / チャンネル一括（サマリ CSV）のどちらがよいか要検討

### 多言語対応（英語）

現状: UI 文言は日本語固定（`src/main.ts` にハードコード）。MSI の `en-US` はインストーラー画面の言語であり、アプリ本体とは別。

- アプリ UI の英語対応（ラベル、ボタン、ログ、エラーメッセージ）
- 表示言語の決め方: OS ロケール追従 / アプリ内切り替え、要検討
- README 等のユーザー向けドキュメントの英語版も対象に含めるか要検討

